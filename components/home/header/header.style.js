import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    button: {
        flexDirection: 'row',
        width: 100,
        marginLeft: 'auto',
        marginRight: 10,
        marginTop: 10,
    },
});

export default styles;