import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    button: {
        width: 100,
        marginLeft: 'auto',
        marginRight: 20,
        marginTop: 10,
    }
});

export default styles;